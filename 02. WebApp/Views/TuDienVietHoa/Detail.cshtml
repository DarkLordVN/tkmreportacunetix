@using TKM.BLL
@model TuDienVietHoaViewModel
@{
    Layout = null;
}

<div class="form-group">
    <div class="row ml-1 mr-1">
        <div class="col-md-5 border border-primary text-justify">
            @(Model != null ? Model.CumTuKhoa : "")
        </div>
        <div class="col-md-2">
            <label class="fwb">=> Thay đổi thành => </label>
        </div>
        <div class="col-md-5 border border-info text-justify">
            @(Model != null ? Model.CumTuThayDoi : "")
    </div>
</div>
</div>
<div class="form-group">
    <div class="row">
        <div class="col-md-12 text-right">
            <button type="button" class="btn btn-danger" data-dismiss="modal">Đóng</button>
        </div>
    </div>

</div>